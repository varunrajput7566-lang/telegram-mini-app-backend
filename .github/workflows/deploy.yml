name: Deploy Backend

on:
  push:
    branches: [ main ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    
    - name: Deploy to Wispbyte
      env:
        DEPLOY_KEY: ${{ secrets.WISPBYTE_DEPLOY_KEY }}
        DEPLOY_URL: ${{ secrets.WISPBYTE_DEPLOY_URL }}
      run: |
        echo "Deploying to Wispbyte..."
